//keep track of a box to dismiss with the press of the button
parent = noone;
hover = false;

//register this in the sound controller so it can make noise
gmwRegisterObject(id, global.DEFAULT_GROUP, "okButton");
