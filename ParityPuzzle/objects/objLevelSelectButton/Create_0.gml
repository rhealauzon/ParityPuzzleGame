// CONSTANTS //
unlocked = false;
completed = false;

// VARIABLES //
//keeps track of what level it is a  button for
levelNum = -1;

//register this in the sound controller so it can make noise
gmwRegisterObject(id, global.DEFAULT_GROUP, "levelSelectButton");
